
database:
  driverClass: org.h2.Driver
  user: sa
  password: sa
  url: jdbc:h2:~/todo-db


flyway:
   # Locations to scan recursively for migrations. (default: db/migration)
  locations:
    - db/migration